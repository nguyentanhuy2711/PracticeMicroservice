server:
  port: 9002

  spring:
    application:
      name: USER-SERVICE

      eureka:
        client:
          register-with-eureka: false
          fetch-registry: false
          service-url:
            defaultZone: http://localhost:8786/eureka/
          instance:
            hostname: localhost